# 🏠 Home Assistant Supervised - ULTIMATE INSTALLER

[![Bash Version](https://img.shields.io/badge/Bash-4%2B-blue)](https://www.gnu.org/software/bash/)
[![License](https://img.shields.io/badge/License-MIT-green)](LICENSE)

Этот скрипт превращает любую ТВ-приставку (X96, Tanix, HK1 на Amlogic/Allwinner), одноплатный компьютер (Raspberry Pi, Odroid) или обычный мини-ПК в полноценный, защищенный и отказоустойчивый сервер Home Assistant Supervised.

Установка HA Supervised на нестандартное железо — сложный процесс, связанный с проблемами в ОС, сетью, AppArmor и обрывами SSH. Этот скрипт решает все проблемы автоматически: от подмены os-release и патча загрузчика до защиты сессии SSH и авто-отката сети.

## ⚠️ Важное предупреждение

Скрипт вносит изменения в сетевой стек (переключает на NetworkManager), ядро (AppArmor) и файрвол. **Обязательно имейте физический доступ (монитор/клавиатура) или UART-подключение к устройству на случай обрыва сети по SSH.** Скрипт имеет систему отката, но 100% гарантию на всех китайских TV-боксах дать невозможно.

---

## ✨ Ключевые особенности

- **🛡️ Умная установка с откатами:** Если шаг установки падает (например, ломается сеть), скрипт автоматически откатывает изменения.
- **🔄 Автопродолжение после ребута:** Если AppArmor требует перезагрузки, скрипт создает systemd-сервис, который продолжает установку с того же места после старта системы.
- **🌐 Бесшовная интеграция Tailscale:** Автоматическая установка, настройка UFW (открытие портов WireGuard и интерфейса tailscale0) и поддержка Auth Key.
- **📶 Надежный Wi-Fi:** Настройка через NetworkManager с принудительным **отключением энергосбережения** (Power Save off), что решает проблему отвала VPN и телеметрии на TV-боксах. Поддержка SSID со спецсимволами и пробелами.
- **🛡️ Безопасность из коробки:** Настройка UFW (включая правила для Docker), Fail2Ban, защита SSH.
- **💾 Продвинутые бэкапы:** Поддержка локальных снапшотов (через HA CLI), загрузка в облако (rclone) или на удаленный сервер (rsync).
- **🚑 Режим восстановления (Rescue):** Автоматическая диагностика и починка файловой системы, сети, Docker и Supervisor.
- **📊 Мониторинг:** Watchdog с экспоненциальной задержкой, термомониторинг, Prometheus метрики.

---

## 📋 Требования

- **ОС:** Armbian (Bookworm / Trixie) или чистый Debian (11/12/13).
- **Архитектура:** `aarch64` (ARM64), `x86_64`.
- **RAM:** Минимум 1 ГБ (рекомендуется 2 ГБ+).
- **Диск:** Минимум 10 ГБ свободного места.
- **Права:** Скрипт необходимо запускать от `root` (`sudo`).

---

## 🚀 Быстрый старт

Самый простой способ запустить установщик — использовать интерактивный мастер:

```bash
curl -fsSL https://raw.githubusercontent.com/iRespect777/HAS-tvbox/refs/heads/main/ha-installer/install.sh -o /tmp/install.sh
sudo bash /tmp/install.sh
```
Мастер проведет вас через выбор профиля, настройку сети, часового пояса и дополнительных компонентов.

---

⚙️ Профили установки
Для удобства скрипт содержит предустановленные профили. Вы можете выбрать один из них или настроить всё вручную (Custom).

Профиль
Описание
Для кого
Minimal	Только HA + Docker. Без файрвола, бэкапов и оптимизаций.	Слабые устройства (1ГБ RAM), эксперименты.
Standard	Рекомендуемый набор: UFW, Watchdog, Бэкапы, ZRAM, Tailscale.	Большинство пользователей. Оптимальный баланс.
Full	Всё из Standard + мониторинг Prometheus.	Мощные боксы (4ГБ+ RAM), гики.
Server	Standard + Статический IP + Tailscale по умолчанию.	Выделенные серверы без монитора.
Dev	HA + HACS, без автоматических перезапусков и защиты.	Разработчики.

📡 Подробно про Tailscale VPN
Удаленный доступ к Home Assistant без открытия портов — лучшая практика безопасности. Скрипт глубоко интегрирует Tailscale:

Авто-установка: Через официальный репозиторий Tailscale.
Авторизация: Поддержка одноразовых и многоразовых Auth Key для headless-авторизации.
Настройка UFW:
ufw allow in on tailscale0 — разрешает весь трафик из вашей частной сети (Tailnet).
ufw allow 41641/udp — открывает порт WireGuard для прямых P2P-соединений (без этого соединение пойдет через relay-серверы с высокой задержкой).
Фикс обрыва Wi-Fi: Автоматически отключает powersave на Wi-Fi адаптерах через NetworkManager. Это решает проблему, когда TV-бокс "засыпает" и Tailscale отваливается.
Установка с авторизацией через ключ:

bash

sudo bash /tmp/install.sh --profile standard --tailscale --ts-authkey tskey-auth-ВАШ_КЛЮЧ
🖥 Режимы работы и CLI аргументы
Скрипт можно использовать не только для установки, но и для управления сервером.

Интерактивные режимы
Просто запустите sudo ha-install (или скрипт) без аргументов, чтобы попасть в главное меню:

Установка
Диагностика (Check)
Мониторинг в реальном времени (Status)
Обновление
Бэкап / Восстановление
Тест железа (Benchmark)
Аргументы командной строки
Аргумент
Описание
-c, --check	Диагностика системы (проверка Docker, HA, сети)
-s, --status	Мониторинг ресурсов и контейнеров в реальном времени (Live)
--rescue	Режим спасения. Автоматическая починка диска, DNS, Docker, сети и Supervisor
--update	Обновление HA Supervised и OS-Agent
--self-update	Обновление самого скрипта установки
--uninstall	Удаление HA (Standard — оставляет Docker/сеть, Full — возврат к чистой ОС)
--benchmark	Тест производительности CPU, RAM и диска с рекомендацией профиля
--profile <ИМЯ>	Указать профиль (minimal, standard, full, server, dev)
--tailscale	Установить и настроить Tailscale VPN
--ts-authkey <КЛЮЧ>	Tailscale Auth Key для автоматической авторизации
--timezone <ЗОНА>	Установить часовой пояс (напр. Europe/Moscow)
--wifi <SSID> <PASS>	Настроить Wi-Fi соединение
--data-dir <ПУТЬ>	Перенести данные Docker и HA на внешний накопитель (SSD/HDD)
--restore-backup <ФАЙЛ>	Восстановить бэкап .tar.gz после установки
--swap <РАЗМЕР>	Настроить swap (zram, 1024, 2048, none)
--auto-reboot	Разрешить скрипту автоматически перезагружать систему (для AppArmor)
--skip-update	Пропустить apt upgrade перед установкой
--dry-run	Тестовый прогон (никаких реальных изменений)
--silent	Тихий режим (минимум вывода)

🛠 Утилиты пост-установки
Скрипт разворачивает набор утилит в /usr/local/bin/ для удобного управления:

ha-health — Вывод сводки о здоровье системы (RAM, Disk, Docker, контейнеры).
ha-backup — Создание бэкапа (полный снапшот через HA CLI, если доступен, или быстрый tar конфига).
ha-restore — Интерактивное восстановление из бэкапа.
ha-notify "Текст" — Отправка уведомления в Telegram или через Webhook (ntfy, Discord, Slack).
ha-watchdog — Следит за HA. При падении перезапускает с экспоненциальной задержкой.
ha-cleanup — Очищает Docker и логи, если на диске меньше 1.5 ГБ свободного места.
ha-metrics — Генерация метрик для Prometheus Node Exporter.
🚑 Решение проблем
1. Скрипт завис или оборвался по SSH
Не паникуйте! Скрипт использует систему состояний (/var/lib/ha-installer/state). Просто переподключитесь по SSH и запустите скрипт снова. Он спросит, хотите ли вы продолжить с прерванного шага.

2. Home Assistant не открывается после установки
Первичная загрузка HA может занимать 10-15 минут (он скачивает образы контейнеров). Проверьте статус:

bash

sudo ha-install --check
Если проблема в сети или Docker, запустите авто-починку:

bash

sudo ha-install --rescue
3. Tailscale отваливается через несколько минут
Это признак того, что ваш Wi-Fi адаптер уходит в спящий режим (Power Save). Скрипт пытается отключить это через NetworkManager, но если это не сработало, выполните вручную:

Узнайте UUID вашего Wi-Fi подключения: nmcli con show
Отключите энергосбережение: sudo nmcli con modify "ИМЯ_СЕТИ" 802-11-wireless.powersave 2
Переподключитесь: sudo nmcli con up "ИМЯ_СЕТИ"
🗑 Удаление
Для удаления используйте аргумент --uninstall. Скрипт предложит два режима:

Standard: Удаляет HA, Supervisor, OS-Agent и контейнеры. Оставляет Docker, NetworkManager и системные пакеты нетронутыми. (Если хотите переустановить HA).
Full: Полная зачистка. Удаляет Docker, настройки сети (возвращает ifupdown), UFW правила, Tailscale, скрипты и логи. Система вернется к состоянию до установки.
📜 Лицензия
Этот проект распространяется под лицензией MIT. Используйте на свой страх и риск. Автор не несет ответственности за слетевшие прошивки TV-боксов.
